# Coding Standards

- TypeScript strict everywhere; avoid `any`.
- Named exports only (avoid default exports).
- One component per file; collocate tests `*.test.ts(x)`.
- Absolute imports via TS aliases; no deep relative paths like `../../..`.
