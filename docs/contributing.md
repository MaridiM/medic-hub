# Contributing

## Branching
- `main` (protected), `develop` (integration), feature branches `feat/<scope>`.

## Commits
- Conventional Commits: `feat:`, `fix:`, `docs:`, `chore:`, etc.

## PR
- Small, focused PRs. Include screenshots for UI, and `docs/ADR-*.md` links if APIs changed.
